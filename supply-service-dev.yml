spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/supply_db
    username: aliyara29
    password: Yara2001
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8090/realms/InovSmart
          jwk-set-uri: http://localhost:8090/realms/InovSmart/protocol/openid-connect/certs
logging:
  level:
    com.aliyara.supplyservice: DEBUG